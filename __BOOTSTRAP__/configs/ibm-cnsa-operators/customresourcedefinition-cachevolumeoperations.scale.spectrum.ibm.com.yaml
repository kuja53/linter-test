apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  labels:
    app.kubernetes.io/instance: ibm-spectrum-scale
    app.kubernetes.io/name: operator
  name: cachevolumeoperations.scale.spectrum.ibm.com
spec:
  group: scale.spectrum.ibm.com
  names:
    categories:
    - scale
    kind: CacheVolumeOperation
    listKind: CacheVolumeOperationList
    plural: cachevolumeoperations
    shortNames:
    - cvo
    singular: cachevolumeoperation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.action
      name: Action
      type: string
    - jsonPath: .spec.cacheVolume
      name: CacheVolume
      type: string
    - jsonPath: .status.conditions[?(@.type=="OperationCompleted")].status
      name: Status
      type: string
    - jsonPath: .status.conditions[?(@.type=="OperationCompleted")].reason
      name: Reason
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CacheVolumeOperation is the Schema for the cachevolumeoperations
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object.

              Servers should convert recognized schemas to the latest internal value,
              and

              may reject unrecognized values.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents.

              Servers may infer this from the endpoint the client submits requests
              to.

              Cannot be updated.

              In CamelCase.

              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            properties:
              action:
                description: action specifies the operation to perform (Download or
                  Evict)
                enum:
                - Download
                - Evict
                type: string
              cacheVolume:
                description: cacheVolume is the name of the associated CacheVolume
                type: string
              contentFilter:
                description: contentFilter specifies the objects for download or eviction
                  based on the specified operation
                items:
                  type: string
                type: array
              contentType:
                default: DataAndMetadata
                description: contentType specifies the type of content for the operation
                  (DataAndMetadata, DataOnly, MetadataOnly)
                enum:
                - DataAndMetadata
                - DataOnly
                - MetadataOnly
                type: string
              ttlDuration:
                default: 24h
                description: 'ttlDuration specifies the time duration to wait before
                  automatically deleting the CVO after successful completion.

                  It can be specified in hours (e.g. 5h) or in days (2h). The default
                  value is 24h.'
                type: string
                x-kubernetes-validations:
                - message: ttlDuration must be a number followed by 'h' (hours) or
                    'd' (days), e.g., '12h' or '3d'
                  rule: self.matches('^[0-9]+[hd]$')
            required:
            - action
            - cacheVolume
            type: object
          status:
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the operation.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: 'lastTransitionTime is the last time the condition
                        transitioned from one status to another.

                        This should be when the underlying condition changed.  If
                        that is not known, then using the time when the API field
                        changed is acceptable.'
                      format: date-time
                      type: string
                    message:
                      description: 'message is a human readable message indicating
                        details about the transition.

                        This may be an empty string.'
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: 'observedGeneration represents the .metadata.generation
                        that the condition was set based upon.

                        For instance, if .metadata.generation is currently 12, but
                        the .status.conditions[x].observedGeneration is 9, the condition
                        is out of date

                        with respect to the current state of the instance.'
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: 'reason contains a programmatic identifier indicating
                        the reason for the condition''s last transition.

                        Producers of specific condition types may define expected
                        values and meanings for this field,

                        and whether the values are considered a guaranteed API.

                        The value should be a CamelCase string.

                        This field may not be empty.'
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              downloadInitiated:
                description: 'DownloadInitiated is a flag that indicates whether the
                  download operation has been triggered.

                  This field becomes true once the download operation has successfully
                  initiated,

                  and remains false if the operation has not yet started.'
                type: boolean
              failedDownloadList:
                description: FailedDownloadList is the list of objects or files for
                  which a download operation has failed. Up to 10 objects or files
                  which failed to be downloaded are shown here.
                type: string
              failedToQueueCount:
                description: FailedToQueueCount is the number of invalid objects or
                  files specified by users which could not be added to the download
                  queue.
                type: integer
              invalidList:
                description: InvalidList is the list of invalid objects or files specified
                  for a download or eviction operation. Up to 10 invalid objects or
                  files are shown here.
                type: string
              retryAttempts:
                description: RetryAttempts specifies the number of retry attempts
                  for the operation.
                type: integer
              totalCount:
                description: TotalCount is the number of objects or files specified
                  for a download or eviction operation.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
